<Window x:Class="ModelHotSwapWorkflow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="模型热插拔工作流平台 - 工业级 ONNX 推理系统" Height="750" Width="1400"
        Background="#1E1E1E">

    <Window.Resources>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Focusable="False" Padding="{TemplateBinding Padding}">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ToolboxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}" 
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}" 
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#505050"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="65"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="180"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D2D30" BorderBrush="Gold" BorderThickness="0,0,0,2">
            <Grid Margin="15,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="12" Height="12" Fill="LimeGreen" Margin="0,0,10,0"/>
                    <TextBlock Text="HVModel" Foreground="Gold" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>

                    <Separator Width="20" Margin="15,10" Background="#555555"/>

                    <RadioButton x:Name="ManualModeRadio" Content="手动模式" Foreground="White" VerticalAlignment="Center" Margin="5" IsChecked="True" Checked="ModeChanged"/>
                    <RadioButton x:Name="TcpTriggerModeRadio" Content="TCP 触发模式" Foreground="White" VerticalAlignment="Center" Margin="5" Checked="ModeChanged"/>
                    <RadioButton x:Name="HttpTriggerModeRadio" Content="上位机模式" Foreground="White" VerticalAlignment="Center" Margin="5" Checked="ModeChanged"/>

                    <Button x:Name="BtnConfig" Content="配置指令映射" Click="OpenTcpMapping_Click" Style="{StaticResource ModernButtonStyle}" Margin="20,0,0,0" ToolTip="设置TCP指令与图像源的对应关系"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnImport" Content="导入工作流" Click="Import_Click" Style="{StaticResource ModernButtonStyle}"/>
                    <Button x:Name="BtnExport" Content="导出工作流" Click="Export_Click" Style="{StaticResource ModernButtonStyle}"/>
                    <Button x:Name="BtnClear" Content="清空画布" Click="ClearCanvas_Click" Style="{StaticResource ModernButtonStyle}"/>
                    <Button x:Name="BtnRun" Content="▶ 运行一次" Click="RunWorkflow_Click" Background="#007ACC" Style="{StaticResource ModernButtonStyle}" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#252526" BorderBrush="#333333" BorderThickness="0,0,1,0">
                <StackPanel>
                    <TextBlock Text="工具箱 (双击添加)" Foreground="#888888" Margin="10,10,0,5" FontSize="12"/>
                    <ListBox x:Name="ToolboxList" Background="Transparent" BorderThickness="0" 
                             ItemContainerStyle="{StaticResource ToolboxItemStyle}"
                             PreviewMouseDoubleClick="ToolboxList_PreviewMouseDoubleClick"
                             PreviewMouseLeftButtonDown="ToolboxList_PreviewMouseLeftButtonDown">
                        <ListBoxItem Tag="ImageSource" Content="📷 图像源节点"/>
                        <ListBoxItem Tag="ModelNode" Content="🧠 模型推理节点"/>
                        <ListBoxItem Tag="Branch" Content="🔀 逻辑分支节点"/>
                        <ListBoxItem Tag="Action" Content="⚡ 动作执行节点"/>
                        <ListBoxItem Tag="DisplayNode" Content="📺 结果显示节点"/>
                    </ListBox>
                </StackPanel>
            </Border>

            <Canvas Grid.Column="1" x:Name="WorkflowCanvas" Background="#1E1E1E" 
                    AllowDrop="True" Drop="WorkflowCanvas_Drop" 
                    MouseLeftButtonDown="WorkflowCanvas_MouseLeftButtonDown">
            </Canvas>

            <Border Grid.Column="2" Background="#252526" BorderBrush="#333333" BorderThickness="1,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="实时推理结果预览" Foreground="White" Margin="10" FontWeight="Bold"/>
                    <Viewbox Grid.Row="1" Margin="5">
                        <Image x:Name="ResultImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor" />
                           
                    </Viewbox>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Background="#2D2D30" Padding="10,2">
                    <TextBlock Text="系统运行日志" Foreground="#AAAAAA" FontSize="11"/>
                </Border>
                <TextBox Grid.Row="1" x:Name="LogTextBox" 
                         Background="Black" Foreground="LimeGreen" 
                         IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="12" BorderThickness="0"
                         Padding="10"/>
            </Grid>
        </Border>
    </Grid>
</Window>