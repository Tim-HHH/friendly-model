<Window x:Class="ModelHotSwapWorkflow.Views.ImageSourceConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="图像采集源配置" 
        SizeToContent="WidthAndHeight" MinWidth="450" MaxHeight="600"
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize"
        Style="{StaticResource StandardDialogStyle}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="图像采集参数设置" FontSize="16" FontWeight="Bold" Foreground="#333" Margin="0,0,0,15"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <GroupBox Header="采集基础设置" Style="{StaticResource StandardGroupBoxStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="采集模式:" Width="70" VerticalAlignment="Center" Foreground="#333"/>
                            <ComboBox x:Name="ModeComboBox" Width="220" SelectedIndex="0">
                                <ComboBoxItem Content="单张文件 (Single File)"/>
                                <ComboBoxItem Content="文件夹遍历 (Folder Sequence)"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,10,0,5">
                            <TextBlock Text="资源路径:" Width="70" VerticalAlignment="Center" Foreground="#333"/>
                            <TextBox x:Name="PathTextBox" Width="220" IsReadOnly="True" Background="#EAEAEA" VerticalContentAlignment="Center"/>
                            <Button Content="浏览" Click="Browse_Click" Width="60" Margin="10,0,0,0" Style="{StaticResource SecondaryButtonStyle}" Padding="5,2"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,10,0,5">
                            <TextBlock Text="采集间隔:" Width="70" VerticalAlignment="Center" Foreground="#333"/>
                            <TextBox x:Name="IntervalTextBox" Width="80" Text="1000" VerticalContentAlignment="Center"/>
                            <TextBlock Text="ms (毫秒)" VerticalAlignment="Center" Foreground="#888" Margin="5,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="预处理设置 (Pre-processing)" Style="{StaticResource StandardGroupBoxStyle}">
                    <StackPanel>
                        <CheckBox x:Name="GrayscaleCheckBox" Content="强制转为灰度图" Foreground="#333" Margin="0,5,0,15"/>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="缩放宽度:" VerticalAlignment="Center" Foreground="#333" Margin="0,0,5,0"/>
                            <TextBox x:Name="ResizeWidthBox" Width="60" Text="0" VerticalContentAlignment="Center"/>

                            <TextBlock Text="缩放高度:" VerticalAlignment="Center" Foreground="#333" Margin="20,0,5,0"/>
                            <TextBox x:Name="ResizeHeightBox" Width="60" Text="0" VerticalContentAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="* 设为 0 则保持原图尺寸" FontSize="11" Foreground="#888" Margin="0,10,0,0"/>
                    </StackPanel>
                </GroupBox>

            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="应用配置" Click="Ok_Click" Width="90" Margin="0,0,10,0" Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="取消" Click="Cancel_Click" Width="70" Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>

    </Grid>
</Window>